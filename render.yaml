services:
  - type: web
    name: fix-the-rangeerror-invalid-time-value-crash-in-the
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: APP_ENV
        value: production
